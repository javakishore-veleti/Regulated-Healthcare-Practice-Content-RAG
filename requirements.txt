# Repo-root Python deps for the conda venv at:
#   $HOME/runtime_data/python_venvs/RHPContent-RAG
#
# Union of every middleware service's runtime deps. Service-level pyproject.toml
# files remain (uv users keep working) but the canonical, conda-driven workflow
# uses this file via the `npm run *` developer commands at the repo root.

# Web stack
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27

# DTO / settings
pydantic>=2.9
pydantic-settings>=2.6

# Postgres (DataMgmt + RAGMgmt)
psycopg[binary,pool]>=3.2

# OpenTelemetry
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-exporter-otlp-proto-http>=1.27

# RAGMgmt-Service specifics
pyyaml>=6.0
anthropic>=0.40
