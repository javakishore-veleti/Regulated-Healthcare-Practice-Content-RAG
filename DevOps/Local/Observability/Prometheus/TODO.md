# Prometheus — local

Status: **placeholder. Image not cached locally — confirm with user before pulling.**

Will scrape the FastAPI middleware services and Airflow's StatsD/Prometheus exporter once the stack is up.
